import Component from '@/routes/reset-password';

export const metadata = {
  title: 'Reset Password | Orchard Beach Community Group',
};

export const dynamic = 'force-dynamic';

export default function Page() {
  return <Component />;
}
